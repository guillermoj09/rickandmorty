urlApi:
  rickAndMorty: https://some-url
  dogCeo: https://some-url

spring:
  datasource:
    url: jdbc:h2:mem:test_db
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
  hibernate:
    ddl-auto: create-drop